name: flutter_hooks_timer

description: Timer with Flutter Hooks.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.3
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.0
  flutter_riverpod: ^0.14.0+3
  font_awesome_flutter: ^9.1.0
  intl: ^0.17.0
  provider: ^6.0.1

dev_dependencies:
  build_runner: ^2.1.2
  dart_code_metrics: ^4.3.3
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.3.0

flutter:
  uses-material-design: true
